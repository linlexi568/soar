watch -n 1 nvidia-smi

不创建文档，直接终端总结

这个为什么脚本里没有参数，我说过在终端传参启动脚本是绝对禁止的，参数必须写在脚本里然后我来改

马上重新使用对齐的奖励新建TD3,SAC，PID，CPID，LQR训练或调参脚本。参数必须写在脚本里然后我来改。同时使soar的训练和测试全部对齐奖励。现在开始办，直到办完为止

深度分析项目，开启搜索，结合相关领域论文，言辞犀利的指出我们的创新点，优势，以及不足与薄弱处（当然这个是初版，主要说项目架构）
